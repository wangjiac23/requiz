# requiz

> re · quiz —— 重复测验，再问一遍。给你一座趁手的题库。

- 版本：v2.0.0（CLI + Localhost 网页 + Obsidian 插件）
- 技术栈：Go + Obsidian Markdown + Agent(pi)
- 运行环境：CLI + Localhost

## 产品背景

对于学生来说，做题的第一步是读题；对于老师来说，做题的第一步是出题。

出题最常见的方式就是拿来别人出的题。别人出的题每年都在增加，有些成为经典，有些已经消失，可能对于老师来说这些题目大部分都是没新意的老题，但对于每一届的学生来说它们都是新题，用别人出的题已经足够了，实在没有必要自己出题。

**出题其实就是选题**。选题最重要的就是数量多、质量高的题目来源。最根本的是课本习题，最权威的是高考真题，其次是各地模考题，再次是机构练习题。

requiz 是一个**题库管理系统**，不是题库平台。题目不是目的，学习才是目的。

## 已实现功能

**v0.0.0（CLI）**
1. 连接题库文件夹（bank）
2. 打开并读取题目文件（Markdown + YAML 元数据）
3. 按需显示题目不同部分（题干 / 答案 / 解析 / 备注）

**v1.0.0（Localhost）**
4. 网页浏览题库（题目列表 + 详情）
5. JSON API（/api/questions、/api/question?id=）

**v1.1.0（Obsidian 化）**
6. 三栏界面：顶部栏（设置 + 题库下拉）· 左侧边栏（题目包树）· 上边栏（标签筛选）
7. 多题库链接：⚙ 设置添加链接题库，下拉切换，持久化到 `.requiz/config.yaml` 的 `links`
8. 新 JSON API：/api/banks、/api/tree，/api/questions 支持 tag/value 筛选

**v1.1.1（修订）**
9. 题库A 增至 10 题（新增 9 题，分 4 个章节包）
10. 设置改为弹出小窗口（修复 CSS 未注入 + 弹窗默认隐藏）
11. 删除页面版本号显示

**v1.1.2（修订）**
12. 左侧边栏 / 上边栏可隐藏：顶栏按钮切换，侧边栏边缘拖拽调宽

**v1.1.3（修订）**
13. 拖拽隐藏：侧边栏拖到 <100px 自动收起，筛选栏拖到 <40px 自动收起（替换双击）
14. 筛选栏新增拖拽调高手柄（40~160px）

**v1.1.4（修订）**
15. 侧边栏/筛选栏新增 📌 固定按钮：固定后拖拽只调尺寸（100~480px / 40~160px）不触发隐藏

**V1.2.0（公式 + 本地联动）**
16. 数学公式渲染（KaTeX 本地化，`$...$`/`$$...$$` 离线可用）
17. 📂 打开本地：资源管理器定位题目 md 文件（explorer /select，目录外路径拒绝）
18. ✏️ 编辑：网页端直接改题干/答案/解析/备注并写回本地
19. ⟳ 刷新：重新扫描题库，同步本地/网页改动（后保存者覆盖先保存者）

**v1.2.1（修订）**
20. 修复公式渲染：题目内容由 `<pre>` 改为 `<div class="content">`（KaTeX auto-render 默认忽略 pre 标签），列表卡片与整页详情同步支持渲染

**v1.2.2（修订）**
21. 网页端可修改题目名（文件名重命名）与元数据（编辑弹窗新增字段，留空删除）

**v1.2.3（修订）**
22. 元数据增强：可新增字段；字段值下拉栏选择（已知值来自 .requiz/meta-values.yaml）
23. 新值二选一：📥 加入配置（全局可选）/ ✍️ 仅本次（只用于该题）

**v1.2.4（修订）**
24. 元数据 UI 优化：添加字段在编辑栏内完成（点按钮展开输入行，不用弹窗）；下拉栏整合「➕ 新增值/✍️ 自定义」；自定义值标 `(自定义)`
25. 每条字段带 ✕ 删除按钮；编辑弹窗可滚动；元数据区可折叠

**V1.3.0（配置拆分）**
26. 配置文件拆分：全局配置 `用户/.requiz/config.yaml`（字段定义/所有题库/默认配置）+ 项目配置 `题库/.requiz/config.yaml`（题库自定义字段）
27. ⚙ 设置升级为配置管理面板（全局/题库两页签 + 配置文件路径显示 + 题库管理 + 字段定义展示）
28. 题库对等化（Obsidian 式）：所有题库无主次，serve 打开的自动注册，下拉平等切换；当前打开的不可移除；旧版 links 自动迁移，路径统一绝对化

**V1.4.0（浏览体验）**
29. 题目盒子：每道题一个盒子默认只显示题干；▼ 展开显示完整内容，答案/解析/备注分段折叠（点击显示）
30. 三模式浏览：📋 列表 / 📑 双栏（左列表+右详情）/ 🃏 卡片（◀▶ 前进后退）
31. ⚙ 字段显示清单（勾选显示字段，本地保存）+ ☆ 收藏（⭐ 收藏/取消，收藏列表过滤，持久化到全局配置）

**v1.4.1（修订）**
32. 题干完整显示（tree API 返回完整 prompt，不再截断第一行）
33. 修复展开 bug：答案/解析/备注按钮阻止冒泡（原点击会收起详情）+ 标题改用 data-title（原 slice 截断错乱）

**v1.4.2（修订）**
34. 展开层级明确：展开题目=一级展开，答案/解析/备注=二级展开；卡片模式自动一级展开

**v1.4.3（修订）**
35. 键盘导航：双栏/卡片模式 ↑← 上一题、↓→ 下一题（输入框聚焦时不触发）

**V1.5.0（导航分区 + 组卷导出）**
36. 左侧导航三分区：题库树 / 收藏 / 自定义清单（页签切换）
37. 收藏落盘项目配置；自定义清单组卷（选题存清单，持久化项目配置）
38. 选择模式：筛选后全选/逐个勾选 → 📋 存清单 / 📤 导出
39. 导出部分可选（题干/答案/解析/备注），格式：JSON / HTML（可打印 PDF / Word）
40. 布局修复：导出弹窗独立小窗口（不占页面）；`#body min-height:0` 修复外部滚动条/底部空白；Cache-Control no-store 防缓存旧页面

**v1.5.1（修订）**
41. 修复收藏星标刷新后不显示：/api/favorites 带 bank 参数 + 补回 dir 字段，key 用 state.bank 统一

**v1.5.2（修订）**
42. 导出后文件操作：📂 打开（默认程序）/ 📍 定位（资源管理器 /select），仅限 output/ 目录（越权拒绝）

**v1.5.3（修订）**
43. 修复导出 HTML 公式不渲染：KaTeX JS 内嵌进导出文件（file:// 下外部 JS 被安全策略禁止），公式自包含渲染

**V1.6.0（代码模块化）**
44. server.go（2534 行）拆分为 8 个模块源文件：store/api/config_api/favorites/export/web/views/assets
45. JS 前端资源（indexJS/indexCSS）抽取为 assets.go 独立 Go 文件
46. 5 包分层：model/parser/config/quiz/web + main 收口（依赖单向无循环）

**V1.7.0（文件夹导航 + 测试模式）**
47. 任意层级文件夹导航：导航树严格按文件系统目录嵌套（递归渲染 + 展开收起）
48. 目录筛选：点击目录主区域只显示该目录题目（含子目录），Ctrl 多选合并，点空白取消
49. 清单升级题组（📁 文件夹样式 + ▶ 测试按钮）；在线测试模式：题目 + 按题型答题区（选择/填空/解答）
50. 测试设置：计分/不计分 + 正计时/倒计时/不计时（倒计时自动交卷）
51. 客观题自动评（归一化比对，容错量词/LaTeX）；主观题用户评分；测试报告（对错/得分/用时/参考答案）

**v1.7.1（修订）**
52. 非测试模式查看题组：点击题组名 → 主区域只显示该题组题目（提示条 + 高亮，点空白取消）

**v1.7.2（修订）**
53. 删除评分功能（用户自评）；做题区统一单一作答区（无选项/题型控件），报告显示我的答案 vs 参考答案

**V1.8.0（图片及附件）**
54. 目录约定：`题库/image/<题目名>/` 存放题目图片与附件
55. 网页端渲染 Markdown 图片 `![alt](image/题目/文件名)`（静态服务 + 路径校验）
56. 📷 上传：网页端上传图片/附件到当前题目文件夹，返回引用语法

**V2.0.0（Obsidian 集成）**
57. Obsidian 插件：新标签页 iframe 加载 localhost requiz 网页（复用全部功能）
58. 插件设置：端口/exe 路径/题库目录 + 「启动 requiz」按钮 + 连接状态检测

## 快速开始

```bash
# 构建
go build -o dist/requiz.exe ./src

# 1. 连接题库文件夹（demo/题库A 是带 .requiz 配置的演示题库）
./dist/requiz.exe connect "demo/题库A"

# 2. 列出题库中的题目（可跟目录参数；cd 进题库目录后可省略）
./dist/requiz.exe list demo/题库A

# 3. 读取单题全文
./dist/requiz.exe read M001 demo/题库A

# 4. 按需显示（缺省显示题干；可用 --a/--e/--n 组合）
./dist/requiz.exe view M001 demo/题库A --a --e

# 5. 启动 localhost 网页服务（v1.0.0 → v1.1.0 多题库 + 三栏 UI）
./dist/requiz.exe serve demo/题库A -port 8080
# 浏览器打开 http://127.0.0.1:8080/ ：
#   顶部栏 ⚙ 设置 → 添加链接题库（写入 demo/题库A/.requiz/config.yaml 的 links）
#   下拉栏切换题库；左侧边栏浏览题目包；上边栏按标签筛选
# JSON API：/api/banks、/api/tree、/api/questions?tag=知识点&value=xxx、/api/question?id=
```

## 项目结构

```
requiz/
├── src/                 Go 源码
├── data/                题库仓库（原始题库，仅存题目）
│   └── math bank/       数学题库（题库/题册/题组/题目 层级）
├── demo/                演示区：从 data 选取题目组织成题库
│   ├── 题库A/           一个可连接的演示题库（题库 = 题目 + .requiz）
│   │   └── .requiz/     题库A 的连接配置（config.yaml，links 链接题库B）
│   └── 题库B/           另一个可连接的演示题库（函数专题，演示下拉切换）
├── test/                测试
├── dist/                构建产物
├── docs/                技术文档
├── output/              导出输出目录
├── README.md
├── LICENSE
└── .github/             CI 配置
```

## 题目文件格式

一个题目就是一个 Markdown 笔记，YAML 元数据（系统属性 + 可选属性）+ 二级标题结构：

```markdown
---
app: requiz            # 运行软件（系统属性）
bank: math             # 所属题库（系统属性）
id: M001               # 题目编号（系统属性）
path: 题册/题组/题目.md # 路径（系统属性）
章节: 第一章            # 以下为可选属性
年级: 高一
难度: ★★
重要性: 必做经典题
来源: 课本习题
知识点: 集合
题型: 选择题
---

## 题目
题干内容……

## 答案
答案内容……

## 解析
解析内容……

## 备注
备注内容……
```

## 目录约定

- `data/` 只存放题库（原始素材，不含配置）；`demo/` 从 data 选取题目作为演示题库
- `.requiz/` 连接配置仅存在于可连接的题库目录下（如 `demo/.requiz/`）
- 一个题库建议收纳不超过 3000 道题，尽量少而精
- 题库层级不超过三层：**题库 → 题册 → 题组 → 题目**
- 题组（一个章节 / 一套题）10~30 道
- 题册（高一上 / 专题）10~30 个题组
- 题库 5~15 个题册